<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import com.possum.ui.common.controls.*?>

<VBox xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.possum.ui.products.ProductsController"
      styleClass="workspace-view"
      stylesheets="@../../styles/inventory.css">

    <!-- Header Area -->
    <HBox styleClass="view-header" alignment="CENTER_LEFT" spacing="20">
        <VBox spacing="4">
            <Label text="Products Library" styleClass="view-title" accessibleRole="TEXT"/>
            <Label text="View, search, and manage your central product catalog and their configurations." styleClass="view-subtitle" accessibleRole="TEXT"/>
        </VBox>
        <Region HBox.hgrow="ALWAYS"/>
        <HBox spacing="12" alignment="CENTER_RIGHT">
            <Button fx:id="refreshButton" text="↺ Refresh List" onAction="#handleRefresh" styleClass="action-button" accessibleText="Refresh products table" accessibleRole="BUTTON">
                <tooltip>
                    <Tooltip text="Reload the latest product data from the database"/>
                </tooltip>
            </Button>
            <Button fx:id="addButton" text="Add New Product" onAction="#handleAdd" styleClass="action-button, primary-button" accessibleText="Create a new product record" accessibleRole="BUTTON" defaultButton="true">
                <tooltip>
                    <Tooltip text="Create a new product and define its variants"/>
                </tooltip>
            </Button>
        </HBox>
    </HBox>

    <!-- Main Content -->
    <VBox styleClass="card" VBox.vgrow="ALWAYS">
        <FilterBar fx:id="filterBar"/>

        <!-- View mode toggle buttons (re-parented by controller) -->
        <HBox fx:id="viewToggleContainer" visible="false" managed="false">
            <ToggleButton fx:id="cardsViewButton" text="Cards" styleClass="toggle-btn-neon" selected="true"/>
            <ToggleButton fx:id="tableViewButton" text="Table" styleClass="toggle-btn-neon"/>
        </HBox>

        <StackPane VBox.vgrow="ALWAYS">
            <ScrollPane fx:id="productsGridScroll" fitToWidth="true" hbarPolicy="NEVER" vbarPolicy="AS_NEEDED" styleClass="product-grid-scroll">
                <content>
                    <FlowPane fx:id="productsGrid" hgap="16" vgap="16" prefWrapLength="1200" styleClass="product-grid">
                        <padding>
                            <Insets top="8" right="4" bottom="8" left="4"/>
                        </padding>
                    </FlowPane>
                </content>
            </ScrollPane>

            <DataTableView fx:id="productsTable" VBox.vgrow="ALWAYS" styleClass="data-table" visible="false" managed="false"/>
        </StackPane>

        <PaginationBar fx:id="paginationBar"/>
    </VBox>

</VBox>
